# 2026-09-14

- 发票管理-申请管理「新增开票申请」弹窗：第三步（核对提交）弹窗高度改为随内容自适应（`.is-review-step` → `height:auto; max-height:92vh`），内容区正常视口下完整展示不滚动；第一、二步布局不变。改动文件：`Prototype/modules/billing-invoice.js`（样式 + `render()` 状态类切换）。
- 发票管理三个列表（申请管理/票据管理/红冲任务）数据表：申请单号、发票抬头（`.primary-cell`）与开票金额（`td.amount`）取消加粗（font-weight:400），仅作用于三张表的 scope 内，其他列与更正申请列表不动。改动位置：`Prototype/modules/billing-invoice.js` 原 2106 行附近样式块。
