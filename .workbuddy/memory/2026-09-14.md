# 2026-09-14

- 发票管理-申请管理「新增开票申请」弹窗：第三步（核对提交）弹窗高度改为随内容自适应（`.is-review-step` → `height:auto; max-height:92vh`），内容区正常视口下完整展示不滚动；第一、二步布局不变。改动文件：`Prototype/modules/billing-invoice.js`（样式 + `render()` 状态类切换）。
- 发票管理三个列表（申请管理/票据管理/红冲任务）数据表：申请单号、发票抬头（`.primary-cell`）与开票金额（`td.amount`）取消加粗（font-weight:400），仅作用于三张表的 scope 内，其他列与更正申请列表不动。改动位置：`Prototype/modules/billing-invoice.js` 原 2106 行附近样式块。
- 按最新原型同步文档：`PRD/财务管理/发票管理PRD.md` 升 V2.0——新增第 29 章「新增开票申请（管理员代客申请）三步弹窗」完整需求（入口/三步字段与校验/资格规则文案/限额提醒/提交契约/权限与安全约束/验收要点），§4.7 补代客申请动作，§28 增补字重与第三步高度自适应基线；`PRD/财务管理/发票管理产品设计方案.md` 升 v1.3——§0 补代客申请结论、§28.1/28.2/28.3 增补字重口径、新增 §28.5 弹窗视觉与交互规格。原型「新增开票申请」三步弹窗此前在两份文档中均无记载，本次补齐。
- 用户列表页 8 项需求落地（`Prototype/modules/user-list.js`，采用模块既有 before 运行时补丁模式，新增 before[1] 补丁）：①批量【修改来源/取消禁用/设置禁用】未勾选时 Toast「请选择用户」；②IP风控等级/IP风控标签、成员数、环境数、套餐数、赠送环境数、赠送成员数列头新增「?」说明气泡；③成员数/环境数/套餐数/赠送环境数/赠送成员数数值右侧「!」明细气泡（按企业逐行表格）；④企业ID/企业名【查看】打开「企业信息」弹窗（对齐图1）；⑤套餐周期/赠送周期两行显示（至 后换行），套餐数两行（10/10⏎1/1）；⑥⑦⑧新增 赠送环境数/赠送成员数/赠送周期 三列（插在套餐周期之后，列宽 nth-child 28-38 已顺延覆盖）。同步新建 `PRD/用户管理/用户列表PRD.md`。技术要点：br() 只匹配字面 `\n`，真实换行需自建 NLRE；normalizeTableHead 会把含换行的 th textContent 重写、连带清掉图标，故 risk 列头改为「IP风控等级/IP风控标签」单行写法；明细浮层 6 列表格需 max-width:820px。已用 agent-browser 全链路验证（toast/气泡/弹窗/换行/38列表头）。

# 2026-09-15 追加
- 用户列表新增两列：用户行业（IP风控等级/IP风控标签 之后，取值 电商/跨境/游戏/社媒/广告）、注销状态（状态 之后，未注销/已注销，已注销红色 status-danger），列宽 nth-child 9-40 全量顺延覆盖，最小宽度 5600px。修改方式：更新 /tmp/ul/patch.js 生成器后幂等重写 user-list.js 的 before[1]（patch.js 为一次性工具未入库，如需再改可重建）。PRD 已同步完整列顺序。需求原文中「是否付费/系统」出现两次按重复笔误处理。已浏览器验证：40 列表头顺序、行业/注销取值、批量守卫回归通过。
