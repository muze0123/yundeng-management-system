# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Stack

static HTML/CSS/JavaScript

## Users

后台运营人员和资源管理人员，负责筛选、查看和维护静态代理资源。

## Product Purpose

提供静态代理资源列表、筛选和配置入口，帮助用户按资源来源、渠道、地区和是否显示快速定位可用资源。

## Operating Context

用户在云登后台中管理代理资源，主要围绕资源列表、筛选条件和表格配置进行操作。

## Capabilities and Constraints

- 需要展示资源来源、渠道、地区、是否显示等列表字段。
- 资源来源包含静态云平台、静态家庭宽带。
- 渠道名称以用户提供的静态清单为准。
- 地区筛选为三级联动下拉。
- 页面沿用现有云登后台静态 HTML 壳与设计系统规范。

## Brand Commitments

云登后台管理系统，中文界面，静态 HTML 原型。

## Evidence on Hand

- 用户提供的两张参考截图。
- 现有 `Prototype/代理列表.html` 页面。

## Product Principles

- 先保证筛选效率，再保证列表信息完整。
- 列表字段以业务管理所需为准，不省略关键状态。
- 交互保持后台工具型页面的克制和清晰。

## Accessibility & Inclusion

界面需保持中文可读性、表格可扫读性和键盘可操作性。

## 静态代理：产品号段与售卖池（2026-09-07）

- 点击列表中的产品名称，打开“号段推荐设置”弹窗；标题右侧提供关闭按钮，内容顶部提供“设置推荐”。
- 号段表格包含选择框、号段、推荐平台、不适合平台、备注；无推荐或备注时显示 `--`。截图示例号段为 `108.165.104.0/24`、`108.165.129.0/24`、`108.165.215.0/24`。
- 列表右下提供总数、每页条数、上一页/页码/下一页、跳页；按截图默认 50 条/页，可切换 10/20/50 条。跳页限制在有效范围，跳转后输入框恢复 1。
- 推断补全：勾选一个或多个号段后可设置推荐平台、不适合平台和备注；未选择时提示先选择。提交更新当前产品的已选号段，取消返回号段列表。平台字段最多 100 字，备注最多 200 字，显示实时字数。各产品配置互不影响，会话内保留，刷新恢复 Mock 数据。
- 弹窗使用现有 Modal 规范，支持关闭按钮、遮罩和 Escape 关闭，关闭后恢复触发控件焦点（鼠标点击售卖池编辑时不恢复按钮焦点，避免残留高亮；键盘触发仍恢复焦点）；编辑推荐时取消/关闭返回号段列表。
- 售卖池列统一使用普通正文文字，不使用彩色字、标签背景或描边；右侧仅保留编辑铅笔图标，确保列宽足够，不出现省略号。保存售卖池后立即刷新列表并保留编辑图标。鼠标关闭弹窗后编辑图标恢复默认灰色与透明背景，仅 hover 时高亮；键盘操作保留可见焦点。
- 验收：产品名称弹窗与三个截图号段一致；全选/单选、推荐保存、取消、产品间隔离、分页边界、售卖池回显均可操作；平板下表格在弹窗内横向滚动。
