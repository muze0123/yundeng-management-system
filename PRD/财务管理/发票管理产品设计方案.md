# 云登后台管理系统 · 发票管理产品设计方案

> 文档版本：v1.1 
> 需求 ID：PRD-20260818-ADMIN-INVOICE 
> 所属模块：财务管理 / 发票管理 
> 需求类型：新建 
> 优先级：P0 
> 目标端：Web PC，1280px 桌面优先，768px 平板可用 
> 文档状态：方案基线已确认 
> 最后更新：2026-08-18

## 0. 文档结论

云登后台新增独立“财务管理”分组，首期包含“发票管理”。该模块以发票申请为工作单元，关联订单、支付、履约和退款事实，完成系统校验、财务人工审核、第三方数电发票开具、票据预览/交付、退款红冲、异常恢复与审计。

发票申请、第三方开具、票据效力和交付是四套正交状态，不得合并为订单的单一“发票状态”，也不得直接复用订单支付或履约状态。

## 1. 背景、目标与边界

### 1.1 当前现状

- 后台现有“订单管理”可查询订单主记录、支付、履约和退款事实。
- 套餐订单 PRD 已将发票、对账单和结算报表划归财务模块，但当前没有财务管理菜单和发票页面。
- 当前没有发票申请、订单分摊、可开票账本、票据、交付、第三方任务和红冲模型。
- 订单状态不能表达审核中、开票失败、红冲失败或邮件失败等发票领域事实。

### 1.2 一句话目标

让具备权限的财务管理员在一个可审计工作台内审核用户申请并安全调用第三方开具数电发票，在并发、超时、退款、红冲和交付失败时保持订单金额与票据结果最终一致。

### 1.3 本期范围

- 财务管理菜单与发票管理路由；
- 发票申请列表、筛选、统计、详情和审核；
- 审核通过后调用第三方电子发票服务平台；
- 蓝票/红票预览、下载、补拉和文件归档；
- 开票失败、结果未知、回调异常和交付失败处理；
- 退款自动生成红冲任务；
- 已开票信息更正申请的审核、红冲和自动生成重开草稿；
- 发票内容、开票主体与第三方账号配置；
- 多角色权限、数据范围、导出、对账与审计。

### 1.4 非目标

- 代替税务数字账户或第三方平台的完整税务管理功能；
- 纸质发票与邮寄物流；
- 无真实订单和支付事实的手工开票；灾备线下登记仍必须关联已通过申请、原开具任务和真实订单分摊；
- 直接修改已开具票面；
- 首期日常申请审核不做自动免审、双人复核或复杂金额风控；仅灾备线下票据登记执行上传人与确认人分离；
- 将发票状态合并进订单状态机；
- 对账单、结算报表和进项发票管理。

## 2. 信息架构与路由

### 2.1 菜单结构

```text
工作台
用户管理
订单管理
 订单列表
 套餐订单
财务管理
 发票管理
资源管理
数据埋点
```

### 2.2 路由

- 稳定模块 key：`invoice-management`。
- SystemFrame 路由：`Prototype/系统框架.html#invoice-management`。
- 模块包：`Prototype/modules/invoice-management.js`。
- 兼容跳转页：`Prototype/发票管理.html`，仅负责保留查询参数并跳转到 SystemFrame，不创建第二套壳层。
- 深链示例：`系统框架.html#invoice-management?application_id=IA202608180001`；实际参数编码方式遵循后台现有路由协议。

新增模块时同步 `Prototype/系统框架.js` 和 `PRD/系统框架PRD.md`；已废弃的 `Prototype/侧栏交互.js` 不是菜单配置源，不同步修改。

### 2.3 模块内 Tab

|Tab|职责|
|---|---|
|申请管理|审核、驳回、查看开具进度与用户申请|
|票据管理|查询蓝票/红票、预览、下载、补拉、交付重发|
|红冲任务|处理退款红冲、用户更正申请、财务纠错、待受票方确认和失败重试|
|开票配置|维护开票主体、发票内容、SKU 税务映射、第三方账号和通知配置|

首期默认进入“申请管理”。配置 Tab 需要更高权限，无权用户不展示。

## 3. 角色、权限与数据范围

### 3.1 角色

|角色|数据范围|职责|
|---|---|---|
|财务审核员|授权开票主体|领取申请、审核、驳回、查看票据与日志|
|财务开票管理员|授权开票主体|审核员能力 + 查询/重试开具任务、关闭失败任务并要求补正、恢复异常排队、补拉、交付重发|
|红冲管理员|授权开票主体|查看和处理红冲任务、查询/登记受票方确认结果、处理失败重试|
|发票更正审核员|授权开票主体|领取、审核或驳回用户更正申请；不得修改用户提交的更正内容|
|财务配置管理员|授权开票主体|维护主体、内容、SKU 税务映射和第三方账号|
|客服/运营只读|授权团队或业务范围|查看用户可见状态和处理建议，不查看完整敏感字段|
|审计员|授权范围|查看详情、票据、操作日志和导出审计，不可写|

### 3.2 权限点

|权限点|说明|
|---|---|
|`invoice.read`|查看申请列表|
|`invoice.detail.read`|查看申请详情|
|`invoice.sensitive.read`|临时查看完整税号、邮箱、电话、银行账号并记录审计|
|`invoice.review.claim`|领取/释放审核任务|
|`invoice.review.approve`|审核通过|
|`invoice.review.reject`|驳回|
|`invoice.issue.queue.resume`|恢复已由审核通过命令创建但因内部队列异常未执行的原任务；不得新建首次开具任务|
|`invoice.issue.retry`|确认未受理后重试|
|`invoice.issue.close_for_correction`|确认未开票后关闭旧任务并要求用户补正；与重试互斥|
|`invoice.provider.query`|主动查询第三方结果|
|`invoice.document.preview`|预览票据|
|`invoice.document.download`|下载票据|
|`invoice.document.refetch`|从第三方补拉文件|
|`invoice.delivery.resend`|重新交付|
|`invoice.red.create`|发起红冲|
|`invoice.red.retry`|红冲失败重试|
|`invoice.red.confirmation.read`|查询第三方受票方确认状态|
|`invoice.red.confirmation.register`|登记线下核验的受票方确认结果与凭证|
|`invoice.correction.read`|查看更正申请及关联链|
|`invoice.correction.claim`|领取/释放更正审核任务|
|`invoice.correction.approve`|通过更正申请并原子创建红冲任务|
|`invoice.correction.reject`|驳回更正申请|
|`invoice.document.offline.register`|灾备模式下上传并登记已在线下开具的票据|
|`invoice.document.offline.confirm`|以不同操作者复核线下票据并使其进入正式票据账本|
|`invoice.config.manage`|维护主体、内容、税务映射和服务商配置|
|`invoice.export`|导出申请/票据数据|
|`invoice.audit.read`|查看完整操作与接口审计|

列表与详情接口返回 `allowed_actions`。页面显示与否由该字段决定，但所有写接口仍需服务端再次鉴权、状态校验和版本校验。

## 4. 申请管理页面

### 4.1 页面布局

页面采用后台统一“两区块”布局：

1. 上方筛选区；
2. 下方数据区，包含状态 Tab、紧凑指标、批量操作、表格和分页。

不新增营销式卡片或重复页面壳层。表格超宽时仅表格区域横向滚动。

### 4.2 状态 Tab

顺序固定：全部、待审核、审核中、开票中、已开票、已驳回、开票失败、红冲处理中、已红冲。

Tab 是客户端派生状态的后台快捷筛选；详情仍展示四维原始状态。交付失败不替代已开票，使用独立筛选和次级 Badge。

### 4.3 筛选字段

|字段|组件与规则|
|---|---|
|申请编号/发票号码/订单号|搜索框，支持精确或前缀匹配，最多 64 字|
|申请状态|多选：待审核、审核中、已通过、已驳回、已撤回、已取消|
|开具状态|多选：未提交、提交中、开票中、部分成功、成功、失败、结果未知|
|票据状态|多选：无票据、正常、红冲中、部分红冲、已红冲、红冲失败|
|交付状态|多选：未发送、发送中、部分送达、已送达、发送失败|
|发票类型|数电普票、数电专票|
|抬头/税号|名称模糊；税号精确，查询日志脱敏|
|开票主体|按管理员数据范围返回|
|发票内容|财务配置枚举|
|申请人/团队|账号、团队名称或 ID；受数据权限约束|
|申请时间|默认最近 30 天，最大跨度 730 天|
|审核人|当前管理员、全部、指定管理员|
|异常类型|配置缺失、第三方超时、回调异常、文件失败、交付失败等|

查询条件按 AND 组合；点击查询回到第 1 页。导出使用不可变查询快照，不读取前端当前表格作为数据源。

### 4.4 指标

|指标|口径|
|---|---|
|待审核|当前授权主体范围内申请状态为待审核的数量|
|审核超时|超过审核 SLA 且未进入已通过/驳回/撤回的数量|
|开票异常|开具失败、结果未知超时、部分开票异常的申请数量|
|待红冲|红冲任务待处理、待确认或失败的数量|
|交付失败|开票成功但全部交付渠道失败的申请数量|

指标数字可点击应用对应筛选。指标接口和列表接口使用同一权限与口径。

### 4.5 列表字段

|字段|说明|
|---|---|
|选择框|仅允许批量领取、批量导出等无副作用或可逐项反馈动作|
|申请编号|等宽字体，可复制，点击打开详情抽屉|
|派生状态|主 Badge + 异常/交付次级 Badge|
|发票类型|数电普票/数电专票|
|受票方|抬头名称 + 主体类型；税号默认不展示|
|申请金额|申请总金额，人民币两位小数|
|开票主体|销售主体简称|
|发票内容|首个内容 + 明细数量|
|关联订单|订单数；点击在详情定位订单区|
|申请人/团队|脱敏账号 + 团队名称|
|申请时间|`YYYY-MM-DD HH:mm:ss`|
|审核人|未领取显示“—”|
|异常摘要|失败原因分类，不直接展示第三方敏感原文|
|操作|查看、领取、审核、驳回、查询结果、重试等 `allowed_actions`|

每页默认 20 条，可选 20/50/100。批量操作最多 100 条，允许部分成功并展示可下载失败明细。

### 4.6 详情、动作与分页契约

- 点击申请编号打开第 5 章详情抽屉，关闭后保留筛选、页码和滚动位置；URL 写入 `application_id`，可复制深链。
- 行动作严格读取 `allowed_actions`：待审核可领取；审核中仅持锁审核人可通过、驳回或释放；开具任务异常可查询结果、重试、恢复原排队任务或关闭补正；成功子任务不显示重试。
- 批量领取仅接收待审核且属于同一授权范围的记录；服务端逐条返回成功、版本冲突、无权限和状态不符结果，页面不得把部分成功提示为全部成功。
- 请求参数固定为 `page`、`page_size`、`sort=application_time_desc`；同一申请时间按 `application_id` 倒序稳定排序。响应返回 `items`、`total`、`page`、`page_size` 和筛选口径时间 `as_of`。
- 翻页和刷新不复用旧 `allowed_actions`；每次列表/详情响应均按当前身份、对象版本和锁状态重新计算。

## 5. 申请详情抽屉

抽屉宽度固定 960px；在 768–1023px 视口下占满可用内容宽度。标题区显示申请编号、四维状态、总金额和主要动作，内容按以下 Tab 组织：

### 5.1 申请与受票信息

- 申请人、团队、申请时间、当前版本和前序申请；
- 发票类型、抬头类型、抬头名称、税号；
- 数电专票附加信息；
- 联系邮箱、联系电话；
- 发票内容选项和财务配置版本；
- 用户备注与超期人工处理标记。

敏感字段默认脱敏；临时查看明文必须有 `invoice.sensitive.read`，二次确认并记录查看审计。

### 5.2 订单与金额分摊

|字段|说明|
|---|---|
|订单号|跳转订单详情，不在抽屉内修改订单|
|业务类型/商品|订单快照|
|支付方式|只允许法币渠道；异常数据标红|
|法币实付|支付服务权威值|
|退款金额|最新确认退款|
|本申请分摊金额|该订单占用金额|
|有效蓝票金额|未被红冲的票据金额|
|可开票余额|最新账本结果|
|资格时间|充值支付成功时间或服务履约完成时间|
|版本|订单和开票账本版本|

详情同时展示“申请时快照”和“当前事实”。差异不自动覆盖申请，由审核规则决定驳回、剔除或继续。

### 5.3 审核信息

- 系统校验结果；
- 领取人、领取时间、审核锁到期时间；
- 审核结论、审核说明、驳回原因码；
- 每次审核版本与操作日志。

### 5.4 第三方任务

- 任务类型：蓝票开具、红冲、状态查询、文件获取；
- 内部 task ID、第三方请求号、受理号；
- 提交时间、最后查询时间、下一次查询时间；
- 请求摘要哈希、响应码、归一化错误码；
- 当前状态、重试次数、人工处置记录。

第三方完整请求/响应仅在受控日志系统中查看，页面不展示密钥、签名、银行账号明文或不必要的票面敏感数据。

### 5.5 票据与交付

- 蓝票/红票列表、原票关联、发票号码、开票日期、金额、税额和状态；
- PDF/OFD/XML 文件状态、哈希、获取时间；
- 站内、邮件等交付状态、次数、最后结果；
- 预览、下载、补拉、重新发送操作。

### 5.6 时间线与审计

按时间倒序展示用户提交、撤回/重提、系统校验、管理员领取、审核、第三方提交/回调、文件获取、交付、退款和红冲。用户可见时间线与内部审计分层；内部错误详情和敏感字段不下发客户端。

## 6. 审核流程

### 6.1 领取审核

- 待审核申请可由有权限管理员领取。
- 领取使用 `application_id + version` 乐观锁；成功后状态变为审核中并记录 `reviewer_id`。
- 默认锁定 30 分钟，可续期；管理员主动释放、超时或账号禁用时回到待审核。
- 领取与用户撤回并发时仅一个版本操作成功；失败方获取最新状态。

### 6.2 审核检查项

系统先自动检查并在详情给出结果：

1. 申请人和团队权限；
2. 订单支付方式和法币实付；
3. 履约资格时点；
4. 退款与可开票余额；
5. 是否存在其他有效占用或票据；
6. 开票主体快照；
7. 发票类型与抬头类型限制；
8. 发票内容与订单税务映射兼容性；
9. 抬头、税号和专票附加字段；
10. 第三方账号、开票额度和配置有效性。

管理员确认系统结果并核对异常差异。不能通过修改页面数据绕过自动校验。

### 6.3 审核通过

确认弹窗展示开票主体、受票方、发票类型、内容、订单数、金额和预计票据数。提交时：

1. 服务端重新鉴权、校验审核锁和申请版本；
2. 再次计算可开票余额和退款事实；
3. 固化最终开票快照；
4. 状态变为已通过，并在同一事务 Outbox 写入唯一“创建首次开具任务”命令；
5. 受控发票服务账号消费命令，幂等创建 ProviderTask 并提交第三方；
6. 页面提示“审核已通过，开票任务将自动提交”，不得提前显示“已开票”。

正常路径中，审核员只需要 `invoice.review.approve`，不需要独立开具权限。首次 ProviderTask 只能由审核通过命令自动创建；发票服务使用受限服务身份执行并记录命令 ID、申请版本和审计日志。若 Outbox/队列异常，财务开票管理员可用 `invoice.issue.queue.resume` 恢复同一个命令/任务，不能手工创建第二个首次任务。

### 6.4 驳回

驳回原因必选，说明按原因决定是否必填：

|原因码|用户文案|是否允许修改重提|
|---|---|---|
|TITLE_INVALID|发票抬头信息有误，请修改后重新提交|是|
|TAX_ID_INVALID|纳税人识别号有误，请核对|是|
|SPECIAL_INFO_INCOMPLETE|专票信息不完整，请补充|是|
|CONTENT_INCOMPATIBLE|发票内容与订单不匹配，请重新选择或拆分订单|是|
|ORDER_CHANGED|订单金额或状态已变化，请按最新信息重新申请|是|
|NO_INVOICEABLE_BALANCE|订单已无可开票金额|否，除非后续红冲恢复|
|DUPLICATE_APPLICATION|订单已存在其他有效申请|否，需查看原申请|
|MANUAL_REVIEW_REQUIRED|该申请需要线下核验，请联系客户支持|转人工工单|
|OTHER|其他原因|说明必填，财务选择是否允许重提|

驳回后释放金额占用，除非原因对应正在确认的第三方结果或红冲冻结；不得由前端自行释放。

## 7. 第三方电子发票平台对接

### 7.1 架构原则

发票服务通过内部统一适配层对接第三方，业务页面不绑定具体供应商品牌：

```text
后台发票管理
 -> 发票领域服务
 -> Provider Adapter（统一请求/响应模型）
 -> 第三方电子发票平台
 -> Callback Inbox（验签、去重、排序、落库）
 -> Polling/Reconciliation（主动查询与日终对账）
 -> Document Vault（票据文件与哈希）
 -> Delivery Service（站内/邮件）
```

### 7.2 提交幂等

- 每次蓝票开具或红冲操作生成稳定 `provider_request_id`。
- 同一业务任务重试必须复用原 request ID，除非已确认第三方未受理且旧任务被关闭。
- 服务端建立 `(provider, seller_entity, provider_request_id)` 唯一约束。
- 第三方返回受理号后立即保存；任何新请求前先按受理号/请求号查询。

### 7.3 回调处理

1. 校验来源、时间戳、签名和重放窗口；
2. 以第三方 event ID 建立唯一约束；没有 event ID 时使用稳定事件指纹；
3. 原始回调先写入 Inbox，再异步归一化处理；
4. 按业务版本和第三方状态序列判断乱序，旧状态不得覆盖终态；
5. 更新 ProviderTask、InvoiceDocument 和开具状态；
6. 记录审计并触发文件获取、交付或异常待办；
7. 回调处理失败可重放，但不得重复生成票据。

### 7.4 超时与结果未知

- 请求超时不等于开票失败，进入“结果未知”。
- 系统按固定退避策略在提交后第 1、3、10、30 分钟主动查询；第 30 分钟仍未知则进入人工核对并继续每 2 小时查询，直至取得终态或人工凭证确认未受理。
- 结果未知期间订单金额保持占用，不允许用户撤回、重提或管理员创建新开票请求。
- 只有第三方明确返回未受理/不存在，或财务在服务商后台核对并记录证据后，才允许关闭旧任务并重试。

第三方明确失败若属于用户资料问题，不得把已通过申请倒退为“已驳回”。有权限管理员可执行“关闭旧任务并要求补正”：

1. 再次查询并上传/记录“未开票”证据；
2. 原 ProviderTask 标记为已关闭且永久禁止重试；
3. 原申请保留 `APPROVED + FAILED`，写入失败处置 `USER_CORRECTION_REQUIRED`；
4. 原子释放未转为蓝票的金额占用；
5. 开放客户端补正入口并通知用户；用户点击后由服务端按来源申请与处置版本幂等创建/读取草稿，新申请重新进入人工审核并关联 `previous_application_id`。关闭事件本身不自动生成草稿。

“重试旧任务”和“关闭后要求补正”是互斥终局动作；任何一个成功后，另一个必须因版本冲突被拒绝。

### 7.5 对账

- 每日至少一次按开票主体拉取第三方开票/红冲结果，与内部任务和票据核对。
- 差异类型：第三方有票内部无票、内部成功第三方无票、金额不一致、状态不一致、文件缺失。
- 差异进入财务待办，不自动删除或覆盖内部记录。
- 对账修复动作必须记录证据、操作者和前后状态。

### 7.6 服务降级

|故障|降级策略|
|---|---|
|第三方不可用|申请仍可提交和审核；通过后任务排队，页面显示“等待开票平台恢复”|
|回调不可用|主动查询兜底；回调恢复后去重处理|
|文件接口失败|保留开票成功状态，创建文件补拉任务|
|邮件服务不可用|站内票据仍可查看；交付任务排队并预警|
|长期不可恢复|开启受控灾备开关，导出审核通过申请供线下处理；线下票据按 15.1 固定执行上传人与确认人分离后回写|

线下处理是灾备能力，不能作为绕过第三方和内部账本的日常通道。

## 8. 票据预览与文件管理

### 8.1 票据管理 Tab

票据管理 Tab 固定包含“正式票据”和“灾备登记”两个次级 Tab；默认进入正式票据。以下 8.1.1–8.1.2 描述正式票据。

#### 8.1.1 筛选

|字段|组件与规则|
|---|---|
|发票号码/申请编号/订单号|单一搜索框，发票号码精确匹配，申请/订单号支持前缀匹配|
|票据方向|多选：蓝票、红票|
|票据效力|多选：正常、红冲中、部分红冲、已红冲、红冲失败|
|文件状态|多选：待获取、可用、部分缺失、获取失败|
|交付状态|多选：未发送、发送中、部分送达、已送达、发送失败|
|发票类型|数电普票、数电专票|
|开票主体/受票方|主体下拉；受票方名称模糊、税号精确且查询日志脱敏|
|开票日期|默认最近 30 天，最大跨度 730 天|
|第三方服务商|按授权主体和已配置服务商返回|

#### 8.1.2 列表、详情与动作

|列表字段|说明|
|---|---|
|发票号码|等宽字体；点击打开票据详情抽屉|
|蓝/红票|红票同时展示原蓝票号码|
|票据效力|票据自身状态；申请级聚合状态仅在详情展示|
|发票类型/内容|数电普票或专票；票面项目摘要|
|含税金额/税额|人民币两位小数，红票显示负向金额|
|开票主体/受票方|主体简称、受票方名称；税号默认脱敏|
|开票日期|第三方确认的票面日期|
|文件|PDF/OFD/XML 可用数量/应有数量|
|交付|站内和邮件聚合状态|
|关联申请/订单|申请编号和订单数，可跳转|
|操作|预览、单文件下载、打包下载、补拉、交付重发、查看原票/红票|

- 详情抽屉固定展示票面摘要、项目明细、原蓝/红票关系、订单分摊、文件版本与哈希、各渠道交付单元、第三方任务、时间线和审计。
- 预览、下载、补拉和重发均按 `allowed_actions` 控制；已红冲票据仍可查看历史原文件，但必须显示醒目的“已红冲”只读标识。
- 打包下载由异步任务生成 ZIP，包内保留原始文件名映射清单与 SHA-256 校验清单；仅打包当前用户有下载权限的文件，部分失败需输出失败明细。
- 默认按开票日期倒序、发票号码倒序稳定排序；每页 20 条，可选 20/50/100，响应包含 `total` 和 `as_of`。翻页保留筛选，关闭详情保留页码与滚动位置。

#### 8.1.3 灾备登记列表

仅开启灾备开关后展示写入口；历史记录始终可查。筛选项固定为登记单号、申请/任务号、外部发票号码、方向、状态、开票主体、上传人、确认人、登记/确认时间。状态为 `PENDING_CONFIRMATION`、`CONFIRMED`、`REJECTED`。

列表字段固定为登记单号、状态、蓝/红方向、外部发票号码、申请/原任务、含税金额、开票主体、文件摘要、事件单号、上传人/时间、确认人/时间、异常摘要和操作。详情展示票面、逐订单/逐原票分摊、第三方查询证据、文件哈希、重复校验及审计；待复核可确认/驳回，已确认可跳转正式票据，已驳回只读。默认登记时间倒序、登记单号倒序，每页 20 条，可选 20/50/100。

### 8.2 预览

- 票据预览固定使用已归档 PDF；只读预览器默认适合宽度，可翻页、缩放、下载，不提供编辑。
- OFD/XML 仅提供原文件下载，不在首期转换。第三方只返回 OFD/XML 且 PDF 缺失时显示票面结构化摘要并创建 PDF 补拉任务，PDF 可用后开放预览。
- 预览头部展示发票号码、蓝/红票、开票主体、受票方、金额、开票日期和文件哈希摘要。
- 预览加载失败时不改变票据状态，提供补拉和下载原文件动作。

### 8.3 文件安全

- 原始文件保存在受控对象存储，启用服务端加密、版本和访问审计。
- 下载使用短时签名 URL，一次授权只允许访问指定票据。
- 文件保存期限由财务/法务配置，不得因第三方过期而丢失内部归档。
- 保存文件哈希、大小、MIME、来源、获取时间；补拉后新版本不静默覆盖旧文件。
- 票据文件不得作为邮件附件长期暴露公共 URL；优先发送受控下载链接或受保护附件。

## 9. 红冲与重新开票

“红冲任务”Tab 内固定包含“红冲任务”和“更正申请”两个次级 Tab。退款自动红冲和财务纠错进入“红冲任务”；用户对已开票信息发起的更正进入“更正申请”，审核通过后才创建红冲任务。

### 9.1 红冲来源

|来源|触发|
|---|---|
|退款自动红冲|退款审批通过且存在有效蓝票|
|用户更正重开|用户因抬头或信息错误提交更正申请并经财务审核|
|财务纠错|发现票面或税务配置错误，具备权限的管理员发起|

### 9.2 红冲任务页面

#### 9.2.1 筛选

|字段|组件与规则|
|---|---|
|红冲任务号/原发票号码/来源业务号|精确或前缀匹配；来源业务号包括退款单号、更正申请号、财务纠错单号|
|任务状态|多选：待处理、待受票方确认、提交中、红冲中、结果未知、部分红冲成功、已红冲、红冲失败、已取消|
|来源类型|退款自动红冲、用户更正、财务纠错|
|开票主体/受票方|受数据范围约束；税号精确匹配且日志脱敏|
|创建/完成时间|分别选择；默认创建时间最近 30 天，最大跨度 730 天|
|确认状态|无需确认、待确认、已确认、拒绝确认、确认超时|
|异常类型|额度不足、信息不一致、第三方拒绝、结果未知、文件失败、交付失败|

#### 9.2.2 列表、详情、动作与分页

|列表字段|说明|
|---|---|
|任务号/状态|任务号可复制，状态显示主 Badge 和异常次级 Badge|
|来源|来源类型 + 来源业务号，可跳转退款单或更正申请|
|原蓝票|原发票号码、票面含税金额|
|目标/成功/占用金额|三列均为含税金额，避免用单一“红冲金额”掩盖部分成功|
|受票方/开票主体|默认脱敏展示|
|确认状态|是否需要受票方确认及最后查询时间|
|第三方进度|受理号、最后查询时间、归一化错误摘要|
|创建/完成时间|精确到秒|
|操作|提交、查询确认、登记确认、查询结果、失败重试、查看红票、取消财务纠错待提交任务|

- 详情抽屉展示来源业务快照、原票及可红冲余额、金额分段、受票方确认凭证、ProviderTask、红票文件/交付、并发占用、时间线和审计。
- 退款自动红冲和已通过用户更正产生的红冲任务不得人工取消；否则会使已通过更正申请失去合法后继状态。仅 `FINANCE_CORRECTION` 财务纠错任务在尚未提交第三方、无成功红冲且未影响已批准退款时允许取消，取消必须在同一事务关闭 RedIntent、将 RedTask 置为 `CANCELLED`、释放对应占用并重新聚合票据状态。
- 结果未知只允许查询；明确失败且确认第三方未生成红票后才显示重试。所有动作传入 `version`，版本冲突后刷新详情。
- 默认按风险优先级、创建时间升序、任务号升序排序；风险优先级固定为结果未知 > 红冲失败 > 待受票方确认 > 待处理 > 处理中 > 终态。每页 20 条，可选 20/50/100。

#### 9.2.3 发起财务纠错

具备 `invoice.red.create` 的管理员可从票据详情点击“发起财务纠错”。弹窗固定填写原蓝票、纠错原因码、目标含税金额、说明和凭证；目标金额可小于或等于当前可红冲余额，不得跨票填写总额。提交后服务端按 12.2 创建 `FINANCE_CORRECTION` RedIntent/RedTask 并返回占用结果。原票无余额、存在结果未知任务或版本冲突时禁止创建并返回阻断对象；管理员不能通过修改金额绕过锁内校验。

### 9.3 红冲流程

1. 锁定原票和关联订单开票账本；
2. 计算全额或部分红冲金额；
3. 判断是否需要受票方确认红字信息；
4. 创建红冲任务并调用第三方；
5. 结果未知时主动查询，不新建重复任务；
6. 成功后保存红票并更新原蓝票关联状态；
7. 按最新退款净额重算订单可开票余额；
8. 若来源为审核通过的用户更正申请，红冲目标全部成功后由系统自动创建一份关联的新开票申请草稿；
9. 交付红票并通知用户。

### 9.4 红冲状态

|状态|进入条件|允许动作|
|---|---|---|
|`PENDING` 待处理|任务已创建，尚未提交第三方|查看；仅排在同票队首时可提交红冲|
|`AWAITING_CONFIRMATION` 待受票方确认|第三方要求受票方确认|提醒用户、登记/查询确认状态|
|`SUBMITTING` 提交中|请求发送中|查看|
|`PROCESSING` 红冲中|第三方已受理|查询结果|
|`UNKNOWN` 结果未知|请求超时或回调缺失|主动查询，不直接重试|
|`PARTIAL` 部分红冲成功|部分票据/金额成功|查看成功红票，处理剩余失败项|
|`SUCCEEDED` 已红冲|目标金额全部成功|查看、交付；符合条件时重开|
|`FAILED` 红冲失败|第三方明确失败|查看原因、在修复后重试|
|`CANCELLED` 已取消|仅财务纠错任务在提交第三方前按规则取消|查看；退款自动红冲与用户更正红冲无取消动作；占用已释放并触发票据状态重算|

退款自动红冲与用户更正红冲不可取消；财务纠错仅可按 9.2.2 的确定条件取消，不保留“通常可/不可”的人工判断口径。

### 9.5 `InvoiceCorrectionRequest` 更正申请

#### 9.5.1 对象与边界

`InvoiceCorrectionRequest` 是用户针对已开具且仍有有效蓝票金额的发票提出的更正审核单，不等同于原开票申请，也不允许直接改写原票。核心字段固定为：

- `correction_request_id`、`source_application_id`、`source_document_ids`、`team_id`、`applicant_id`、`seller_entity_id`；
- `reason_code`、用户说明、原票快照、拟更正的发票类型/抬头/税号/发票内容/联系信息快照；
- `target_red_amount`，首期必须等于所选原票当前有效蓝票金额，不支持用户自定义部分更正金额；
- `status`、`version`、`reviewer_id`、审核锁、审核结论、驳回原因；
- `red_task_ids`、`replacement_application_id`、`created_at`、`submitted_at`、`reviewed_at`、`completed_at`。

创建入口仅对 `source_application.issuance_status=SUCCEEDED` 且所选蓝票仍有有效金额的场景开放；部分开票、结果未知、红冲中或已全额红冲均拒绝创建。同一更正申请内的原票必须属于同一开票主体；跨主体时客户端按主体分别创建更正申请。

开具失败但未生成任何票据的 `close_for_correction` 不创建该对象，也不自动创建草稿：该场景只关闭原 ProviderTask、释放占用并开放客户端“创建补正草稿”入口；用户点击后由服务端按来源申请与处置版本幂等创建/读取草稿，确认修改后提交关联的新申请。`InvoiceCorrectionRequest` 专用于“已存在有效蓝票，必须先红冲再重开”。

更正单提交后业务字段形成不可变版本快照；每次领取、释放、通过、驳回、撤回及系统状态推进均递增 `version`。驳回后用户修改会创建新 `correction_request_id`，并以 `previous_correction_request_id` 形成版本链，原记录和审核意见永久保留。

每张蓝票设置唯一有效更正槽：`PENDING_REVIEW`、`REVIEWING`、`APPROVED`、`RED_PROCESSING`、`RED_FAILED` 均占用该槽，`REJECTED`、`WITHDRAWN`、`CANCELLED`、`COMPLETED` 释放。同一蓝票重复创建时不插入新记录；对有权用户返回现存更正申请 ID 并定位详情，对无详情权限用户只返回“该发票已有更正处理中”。

#### 9.5.2 状态机

|状态|进入条件|允许动作|
|---|---|---|
|`PENDING_REVIEW` 待审核|用户提交且系统校验通过|领取；用户可在未领取前撤回|
|`REVIEWING` 审核中|更正审核员领取成功|持锁审核员通过、驳回、释放；超时自动回待审核|
|`APPROVED` 已通过|复核通过，并在同一事务创建红冲意图/任务|只读；等待红冲处理|
|`RED_PROCESSING` 红冲处理中|任一关联红冲任务已提交或等待确认|查询进度，不得修改更正信息|
|`RED_FAILED` 红冲失败|任一目标金额明确失败且尚未恢复|处理具体红冲任务；更正单不倒退为待审核|
|`COMPLETED` 已完成|全部目标蓝票红冲成功且重开草稿创建成功|查看关联红票和新草稿|
|`REJECTED` 已驳回|财务审核不通过|用户修改后创建新的更正申请版本链，不复用原 ID|
|`WITHDRAWN` 已撤回|用户在未领取前撤回|只读；重新发起使用新 ID|
|`CANCELLED` 已取消|审批前原票因退款红冲等事实已无可更正余额|只读；通知用户最新票据结果|

`APPROVED -> RED_PROCESSING -> RED_FAILED/COMPLETED` 由关联 RedTask 与重开草稿事实驱动；更正审核员不得手工改写。若多个原票拆成多个 RedTask，任一仍在处理即为 `RED_PROCESSING`，无处理任务且有失败为 `RED_FAILED`；失败任务合法重试后回到 `RED_PROCESSING`。全部目标金额成功后仍保持“红冲成功，草稿生成中”，直至唯一重开草稿创建成功才进入 `COMPLETED`。

系统在待审核/审核中阶段收到退款或其他红冲事实时立即重算：目标有效蓝票金额归零则以状态命令转 `CANCELLED` 并释放审核锁；仍有金额但与提交快照不一致则保留当前状态并标记 `AMOUNT_CHANGED`，审核通过接口必须返回冲突，由审核员按 `REFUND_OR_RED_CONFLICT` 驳回。系统不得静默缩减用户申请的全额更正目标。

#### 9.5.3 审核规则

1. 领取使用 `correction_request_id + version` 乐观锁，锁定 30 分钟；主动释放、超时和账号禁用均回到待审核。
2. 审核通过前重新校验申请人权限、原票效力、当前有效蓝票金额、退款/红冲占用、开票主体、拟更正内容合法性和第三方红冲能力。
3. 审核员只能通过或驳回，不得替用户修改抬头、税号、类型、内容或金额。需修改时使用明确驳回原因，由用户创建新更正申请。
4. 通过事务同时固化审核快照、将状态置为 `APPROVED`、按原票拆分创建幂等红冲任务并占用可红冲余额；任一原票占用失败则整体不通过，返回最新差异。
5. 驳回原因固定为：`NO_EFFECTIVE_BLUE_BALANCE`、`REFUND_OR_RED_CONFLICT`、`CORRECTION_INFO_INVALID`、`CONTENT_INCOMPATIBLE`、`RED_NOT_SUPPORTED`、`DUPLICATE_CORRECTION`、`OTHER`；`OTHER` 必填说明。
6. 驳回、撤回和审批前取消不创建红冲任务；已通过后不得撤回，后续只能处理关联红冲任务。

#### 9.5.4 红冲成功后自动重开草稿

- 当且仅当该更正申请关联的全部目标金额红冲成功，`invoice.red.completed` 消费者以 `correction_request_id` 为幂等键自动创建一个 `application_status=DRAFT` 的新开票申请；不得要求用户再次点击“生成草稿”。
- 新草稿继承原订单分摊、开票主体和经审核的拟更正信息，写入 `source_correction_request_id`、`previous_application_id`、原蓝票/红票 ID 链；创建时按最新退款事实保存每笔订单当前可重开金额，但不占用开票余额。
- 草稿创建与更正申请转 `COMPLETED` 使用 Outbox 保证最终一致；消费重试只能返回同一个 `replacement_application_id`，禁止生成多份草稿。
- 用户进入新草稿后必须确认当前信息并提交；提交时按最新退款、余额、12 个月自助期限和税务配置重新校验，不能因已通过更正审核跳过开票申请人工审核。
- 红冲后仅剩部分余额时草稿只保留当前有效分摊；余额为 0 时仍创建并保留草稿及原因快照，但标记为不可提交并提供人工处理入口。
- 草稿创建成功发送站内通知和邮件；若创建暂时失败，更正申请保持 `RED_PROCESSING` 的“红冲成功，草稿生成中”子状态并自动重试，超过 10 分钟进入后台告警，不得把红冲事实回退为失败。

#### 9.5.5 更正申请次级 Tab

|能力|研发规则|
|---|---|
|筛选|更正申请号、原发票号码、状态、原因、开票主体、申请人/团队、申请/审核时间、审核人|
|列表字段|更正申请号、状态、原票数、目标金额、更正项摘要、申请人/团队、申请时间、审核人、红冲进度、新草稿编号、异常摘要、操作|
|详情|原票与原申请、变更前后字段 Diff、金额与占用、系统校验、审核、关联红冲任务、新草稿、通知、时间线和审计|
|动作|领取、释放、通过、驳回、查看红冲任务、查看新草稿；全部由 `allowed_actions` 下发|
|分页|默认申请时间倒序、更正申请号倒序；每页 20 条，可选 20/50/100，返回 `total`、`as_of`|

## 10. 开票配置

### 10.1 开票主体

|字段|说明|
|---|---|
|主体 ID/名称/简称|稳定 ID；历史订单保存快照|
|纳税人识别号|加密存储，列表脱敏|
|注册地址/电话/银行信息|按第三方和票面要求配置|
|支持发票类型|数电普票、数电专票|
|第三方服务商/账号|引用密钥配置，不在页面显示密钥明文|
|月度额度/预警阈值|用于开票前提示和运营预警|
|状态|草稿、启用、停用；停用不影响历史票据查询|
|生效时间|配置版本化，订单支付时固化主体快照|

### 10.2 发票内容选项

|字段|说明|
|---|---|
|内容 ID/展示名|用户可见的有限选项|
|标准开票名称|实际票面项目名称|
|税收分类编码|财务确认，用户不可见或只读|
|税率/征收率|版本化配置|
|适用开票主体|至少一个主体|
|适用业务/SKU|代理、套餐、续费、充值及具体 SKU|
|适用发票类型|普票、专票或全部|
|有效期/状态|按申请提交时生效版本校验|

内容配置发布前执行覆盖检查：所有在售且支持法币支付的 SKU 必须能唯一或明确地匹配至少一个合法选项。冲突或缺失阻止发布并生成差异清单。

### 10.3 第三方配置

- 服务商标识与接口版本；
- 沙箱/生产环境；
- 开具、查询、红冲、文件获取、回调能力开关；
- 超时、查询退避和人工升级阈值；
- 回调验签证书版本与轮换时间；
- 错误码归一化映射；
- 邮件模板、发送频率和告警接收人。

密钥和证书由密钥管理系统托管，页面只显示掩码、版本和最后验证时间。

### 10.4 开票配置 Tab 页面契约

开票配置 Tab 固定包含“开票主体”“发票内容”“SKU 税务映射”“第三方服务商”“通知与 SLA”五个次级 Tab，仅 `invoice.config.manage` 可新增、编辑、发布、停用；审计员只读。

|次级 Tab|筛选|列表字段|详情/动作|
|---|---|---|---|
|开票主体|名称/税号、状态、支持发票类型、服务商|主体 ID/简称、脱敏税号、发票类型、服务商、额度、预警阈值、当前版本、状态、生效时间|查看版本；新增草稿、编辑草稿、校验、发布、停用|
|发票内容|展示名/标准名称/税收编码、主体、业务/SKU、发票类型、状态|内容 ID/展示名、标准名称、税收编码掩码、税率、适用范围、版本、状态、有效期|查看引用 SKU；新增、复制版本、校验覆盖、发布、停用|
|SKU 税务映射|SKU ID/名称、业务类型、主体、内容、覆盖状态|SKU、业务、支付方式、主体、允许内容数、默认内容、配置版本、覆盖结果|查看差异；编辑草稿、批量导入校验、发布|
|第三方服务商|服务商、环境、能力、账号状态|服务商、接口版本、环境、主体数、能力开关、证书版本、最后验证、状态|连接测试、查看错误码映射；编辑非密钥配置、轮换密钥引用、启停|
|通知与 SLA|模板/场景、渠道、状态|场景、站内/邮件开关、模板版本、审核/异常阈值、接收组、状态|预览模板、发送测试、编辑草稿、发布|

- 列表统一按更新时间倒序、稳定 ID 倒序；每页 20 条，可选 20/50/100。主体、内容、SKU 映射和服务商配置均返回 `version` 与 `allowed_actions`。
- 编辑在右侧抽屉完成；发布前先调用服务端校验，展示受影响在售 SKU、历史申请和待处理任务。已发布版本不可原地修改，只能复制新版本并设置生效时间。
- 停用主体、内容或服务商不会修改历史快照；若仍被待审核申请或未完成任务引用，系统拒绝立即停用并返回阻塞对象清单。
- 批量导入 SKU 映射仅接收模板文件，经“上传 -> 字段校验 -> 影响预览 -> 确认发布”四步完成；任何错误行阻止整批发布，不提供跳过错误行选项。

## 11. 四维状态与派生状态

### 11.1 原始状态

|维度|状态值|
|---|---|
|申请 `application_status`|DRAFT、PENDING_REVIEW、REVIEWING、APPROVED、REJECTED、WITHDRAWN、CANCELLED|
|开具 `issuance_status`|NOT_SUBMITTED、SUBMITTING、ISSUING、PARTIAL、SUCCEEDED、FAILED、UNKNOWN|
|票据 `document_status`|NONE、ACTIVE、RED_PENDING、PARTIALLY_RED、FULLY_RED、RED_FAILED|
|交付 `delivery_status`|NOT_SENT、SENDING、PARTIALLY_DELIVERED、DELIVERED、FAILED|

以上四项是申请级原始维度。单张 `InvoiceDocument` 另保存 `direction=BLUE/RED`、文件状态、交付单元及与原票的金额关联；单票效力按相同红冲事实计算，再依 11.4 聚合为申请级 `document_status`。页面和接口不得把单张票状态直接覆盖申请级聚合值。

### 11.2 ProviderTask 聚合规则

ProviderTask 状态是开具事实源，申请级 `issuance_status` 每次按全部子任务重算：

1. 全部子任务成功：`SUCCEEDED`；
2. 至少一个成功，且仍有任一非成功任务：`PARTIAL`；
3. 没有成功任务，且任一任务结果未知：`UNKNOWN`；
4. 没有成功/未知，且任一任务开具中：`ISSUING`；
5. 没有成功/未知/开具中，且任一任务排队或提交中：`SUBMITTING`；
6. 全部任务明确失败或已关闭且没有成功：`FAILED`；
7. 尚未创建任何任务时按审批与 Outbox 事实优先判定：申请尚未审核通过且未写开具 Outbox 为 `NOT_SUBMITTED`；审核通过事务已写开具 Outbox、等待消费或补偿为 `SUBMITTING`，不得因 ProviderTask 尚未创建而回退。

部分成功时，成功任务对应金额立即从申请占用转为有效蓝票，失败/未知/处理中任务金额继续占用。后台展示成功/总任务数和金额；查询、重试、关闭补正只作用于具体子任务，绝不重试已成功任务。

### 11.3 多票据金额口径

申请、原蓝票和红冲任务均以含税金额为主金额，单位为分，服务端使用整数计算；页面统一格式化为人民币两位小数。申请级金额按其全部 `InvoiceDocument` 和有效红冲占用实时聚合：

|字段|计算口径|
|---|---|
|`issued_blue_amount`|所有已确认成功的蓝票含税金额之和；撤销内部记录不得减少该事实金额|
|`successful_red_amount`|所有已确认成功且关联到本申请蓝票的红票绝对值之和|
|`effective_blue_amount`|`max(issued_blue_amount - successful_red_amount, 0)`|
|`red_reserved_amount`|未释放的待处理、待确认、提交中、红冲中、结果未知和失败待处置红冲分摊金额之和|
|`redable_balance`|`max(issued_blue_amount - successful_red_amount - red_reserved_amount, 0)`|
|`undocumented_occupied_amount`|已通过/开具中的申请分摊金额减去已经转为成功蓝票的金额；失败待处置仍占用|

- 蓝票部分成功时，`issued_blue_amount` 只累加成功子任务；未成功部分保留在 `undocumented_occupied_amount`，不得同时计入蓝票和占用。
- 红冲成功时，将对应金额从 `red_reserved_amount` 原子转入 `successful_red_amount`，两者不得在同一时点重复扣减可红冲余额。
- 红票金额按绝对值参与聚合，票面展示保留负号；税额、未税金额按同样的文档关联口径分别聚合，不用含税金额倒推。
- 任一红冲累计不得超过其原蓝票金额；跨多张蓝票的红冲必须保存逐票 `InvoiceRedAllocation`，不能只保存申请级总额。

### 11.4 `document_status` 多票据聚合算法

每张蓝票先根据关联红冲任务和成功红票计算自身效力，申请级 `document_status` 再按全部蓝票聚合，优先级固定如下：

1. 没有成功蓝票：`NONE`；
2. 任一蓝票存在未关闭的红冲失败任务：`RED_FAILED`；
3. 无红冲失败，任一蓝票存在待处理、待确认、提交中、红冲中或结果未知任务：`RED_PENDING`；
4. `issued_blue_amount > 0` 且 `effective_blue_amount = 0`：`FULLY_RED`；
5. `successful_red_amount > 0` 且 `effective_blue_amount > 0`：`PARTIALLY_RED`；
6. 其余存在成功蓝票的情况：`ACTIVE`。

该优先级只决定主 Badge。例如两张蓝票中一张已全额红冲、另一张仍正常，申请级为 `PARTIALLY_RED`；已有部分红冲且剩余金额正在红冲时为 `RED_PENDING`，详情必须同时展示已成功、处理中和剩余金额。关闭失败任务并释放占用后立即重算，不保留过期的 `RED_FAILED`。

### 11.5 `delivery_status` 多票据聚合算法

- 每张成功蓝票和红票各生成两个当前必需 `DeliveryUnit`：站内文件交付、当前有效邮箱版本交付；唯一键为 `(invoice_document_id, channel, recipient_version)`。同一收件人版本重发只新增 Attempt，不新增必需单元；改用新邮箱时为邮件渠道创建新版本并原子标为当前，旧版本仅保留审计，不进入当前聚合，也不覆盖申请快照。
- 每个单元按全部 Attempt 归一为 `NOT_SENT`、`SENDING`、`DELIVERED`、`FAILED`；晚到的旧 Attempt 不得覆盖更新版本的成功结果。
- 申请级状态只按各票据、各必达渠道的当前有效 `recipient_version` 重算：全部成功为 `DELIVERED`；至少一个成功但未全部成功为 `PARTIALLY_DELIVERED`；无成功且存在发送中为 `SENDING`；无成功/发送中且存在失败为 `FAILED`；全部当前单元均无尝试为 `NOT_SENT`。
- 新增红票或补登记票据会新增必需单元并立即触发重算。若历史已有成功单元而新增单元未完成，原 `DELIVERED` 固定转为 `PARTIALLY_DELIVERED`；只有当前集合没有任何成功单元且存在发送中单元时才为 `SENDING`。历史成功单元本身不回退。
- 文件补拉失败不直接改变 `delivery_status`，但对应单元在文件可用前保持 `NOT_SENT`；票据详情另显示文件异常，防止将“未取得文件”误判成“邮件失败”。

### 11.6 后台派生状态优先级

1. 红冲失败/红冲处理中；
2. 部分开票异常/开票失败/结果未知；
3. 已开票；
4. 开票中；
5. 已驳回；
6. 审核中；
7. 待审核；
8. 已撤回/已取消。

派生状态仅用于筛选和 Badge，不能丢失原始状态。相同派生状态下可有不同 `allowed_actions`。

## 12. 并发、幂等与一致性

### 12.1 订单金额占用

- `InvoiceableBalance` 以订单为粒度保存法币实付、退款、占用、有效蓝票和版本。
- 用户提交先创建唯一 submission 信封；每个 `group_fingerprint` 是独立原子组，各组按指纹确定性编排，组内订单按订单 ID 排序加锁以避免死锁。
- 唯一约束保证同一订单的有效占用与蓝票合计不超过净可开票金额。
- 已通过但开具失败的占用只能在 ProviderTask 已确认未开票并完成 `close_for_correction` 后释放；关闭后的旧任务不可恢复或重试。
- `(submission_id, group_fingerprint)` 建立唯一约束：组内全成或全败，组间允许部分成功；重复相同信封只返回已存结果并续跑未处理组。
- 同一 submission_id 的请求载荷哈希必须一致；用户修正失败组时创建新 submission_id，只携带失败组并关联 parent_submission_id，成功组不得再次进入。
- 有效申请占用按订单分摊是否已释放/已转蓝票判断，覆盖待审核、审核中、已通过待提交、提交中、开票中、UNKNOWN 与 FAILED 待处置，不得仅依赖申请状态枚举。

### 12.2 红冲幂等、并发与可红冲余额

#### 12.2.1 来源业务键

- 每次需要红冲先写入 `RedIntent`，固定来源类型为 `REFUND`、`USER_CORRECTION`、`FINANCE_CORRECTION`，来源业务 ID 分别为退款单 ID、更正申请 ID、财务纠错单 ID。
- 来源业务幂等键固定为 `(source_type, source_business_id, invoice_document_id)`；同一来源事件重复消费返回原 `red_intent_id/red_task_id`，不得生成第二份占用或第二个第三方请求。
- 一个来源跨多张蓝票时按原票拆分 RedIntent；目标总额等于来源业务要求红冲的含税金额，保存确定性的逐票分摊顺序和分摊快照。
- RedTask 重试始终复用 `red_task_id` 和 `provider_request_id`。只有已用第三方查询或人工凭证确认“未受理/未生成红票”，并将旧任务关闭后，才可为同一 RedIntent 创建递增 `attempt_no` 的后继任务；旧任务永久不可恢复。

#### 12.2.2 锁内占用与累计约束

创建或调整红冲分摊必须在同一数据库事务中按 `invoice_document_id` 升序锁定 `InvoiceDocumentRedBalance`：

1. 读取原蓝票含税金额、累计成功红冲、当前有效红冲占用和 `version`；
2. 计算 `可红冲余额 = 原蓝票金额 - 累计成功红冲 - 当前有效红冲占用`；
3. 校验本次分摊金额大于 0 且不超过可红冲余额；
4. 写入 `InvoiceRedAllocation` 并增加 `reserved_red_amount`；
5. 写入 RedTask/Outbox，递增版本后提交事务。

有效占用覆盖待处理、待受票方确认、提交中、红冲中、结果未知、部分成功剩余部分和失败待处置金额；已成功部分转入 `successful_red_amount`，已取消或有未受理证据的已关闭部分释放。数据库层通过余额行和版本更新保证任一时点：

`successful_red_amount + reserved_red_amount <= issued_blue_amount`

不得先查余额再异步占用，也不得以申请级总额替代逐票锁。跨多张票任一分摊失败时整组事务回滚，不产生部分占用。

#### 12.2.3 同票任务串行与退款并发

- 同一蓝票可以存在多个已占用的待处理 RedTask，但任一时点最多一个任务进入待受票方确认、提交中、红冲中或结果未知；其余任务保持待处理并显示“等待前序任务”。选择顺序固定为来源优先级 `REFUND > FINANCE_CORRECTION > USER_CORRECTION`，同优先级按创建时间和任务 ID 升序。
- 前序任务成功、取消或确认未受理后，系统锁定余额行重算后继任务；所需金额已被其他成功红票覆盖时，将 RedIntent 标记为“已由关联红票满足”并释放其未使用占用，不再调用第三方。
- 退款审批不得因已有红冲任务而阻塞退款款项支付。若当前无足够可红冲余额，先幂等记录 `REFUND` RedIntent 为“等待前序对账”且不超额占用；前序任务完成后按最新有效蓝票与退款净额决定补充占用或标记已满足，并进入财务待办。
- 用户更正和财务纠错审批时余额不足则审批事务失败并返回冲突，不创建无占用任务；管理员刷新后按最新原票状态处理。
- 回调、主动查询和人工登记竞争时均以 RedTask `version` 和第三方事件幂等键串行归一化；终态不得被迟到状态覆盖。

### 12.3 管理员并发

- 领取审核使用版本号和审核锁。
- 审核通过/驳回只能由持锁管理员执行；锁过期需重新领取。
- 同一申请的审核、撤回、退款剔除和开票提交均通过状态机命令执行，禁止直接更新状态字段。

### 12.4 事件幂等

|事件|幂等键|
|---|---|
|客户端提交分组|`submission_id + group_fingerprint`；信封另校验 payload_hash|
|领取/审核/驳回|`request_id + application_version`|
|第三方开具|`provider_request_id`|
|第三方回调|`provider_event_id` 或稳定事件指纹|
|退款联动|`refund_id + invoice_document_id`；退款金额写入 payload_hash 校验，不作为可变幂等键|
|红冲意图|`source_type + source_business_id + invoice_document_id`|
|红冲提交/重试|`red_task_id + attempt_no`，第三方复用该任务的 `provider_request_id`|
|更正申请审核|`request_id + correction_request_id + version`|
|受票方确认登记|`red_task_id + confirmation_evidence_hash`|
|线下票据登记|`provider + seller_entity_id + external_invoice_number`，同时校验文件哈希|
|邮件重发|`delivery_request_id`|

## 13. 异常处理矩阵

|异常|系统状态|管理员动作|用户表现|
|---|---|---|---|
|订单提交前已退款|申请校验失败或剔除订单|无需人工，必要时查看日志|提示金额已变化并重新确认|
|审核时余额变化|保留审核中并标记差异|驳回或按规则剔除后重审|审核中/被驳回|
|第三方明确拒绝|申请仍为已通过，开具失败|修复配置后重试；资料错误则确认未开票后关闭旧任务并要求补正|开票失败；允许补正时显示“修改并重提”|
|第三方超时|结果未知|查询第三方，不直接重试|开票中，正在确认结果|
|重复/乱序回调|Inbox 去重或忽略旧状态|仅审计，无业务动作|无感知|
|部分票据成功|部分开票异常|处理失败子任务，成功票不可重开|可下载成功票，异常处理中|
|票据文件缺失|开票成功 + 文件异常|补拉文件|已开票，文件暂不可用|
|邮件失败|开票成功 + 交付失败|重发或修改本次接收邮箱|已开票，可站内下载|
|退款后红冲失败|红冲失败，金额冻结|修复后重试/人工升级|退款已处理，红冲处理中/失败|
|需要受票方确认|待受票方确认|提醒、查询确认结果|显示待办与时限|
|更正审核时原票金额变化|更正单保留当前状态并标记冲突，金额归零则取消|按固定原因驳回或查看系统取消结果|提示原票已变化，按最新票据重新处理|
|更正红冲全部成功|更正单完成，唯一新申请草稿自动创建|只读查看关联链|收到新草稿入口并确认提交|
|红冲成功但草稿暂未创建|红冲事实保持成功，更正单显示草稿生成中|系统重试；超过 10 分钟告警|显示处理中，不重复发起更正|
|同票并发红冲|后继任务待处理/等待前序，不超额占用|查看队列；不得绕过顺序手工提交|退款不阻塞，更正进度按事实更新|
|线下票据号码或文件重复|灾备登记校验失败，不入正式账本|驳回登记并核对第三方|状态不提前变为已开票|
|配置停用|新申请/开具被阻止|启用新版本或驳回|待审核或配置异常|

## 14. 通知与 SLA

### 14.1 用户通知

首期固定使用站内通知 + 申请快照联系邮箱；联系电话只用于人工联系，不发送短信、不用于营销。每个通知以 `event_id + template_version + recipient` 去重，邮件失败不回滚业务状态。

|事件|站内/邮件内容与深链|
|---|---|
|申请提交成功|申请编号、金额、提交时间；进入客户端发票详情|
|审核通过/驳回|结论、可公开原因和下一步；驳回进入可修改副本|
|开票成功|发票号码、金额、站内下载入口；邮件包含受控链接|
|开票失败需补正|公开错误原因、补正入口；不暴露第三方原始响应|
|更正申请提交|更正申请号、原发票号码、目标金额；进入更正详情|
|更正申请通过|说明已进入红冲，原票暂不可按更正完成处理；进入进度详情|
|更正申请驳回|固定原因、财务说明和重新发起入口|
|待受票方确认|确认事项、第三方时限、确认入口；到期前 24 小时站内/邮件各提醒一次|
|更正红冲失败|显示“财务处理中”，不引导重复提交；进入原更正详情|
|更正完成|红票结果和系统自动生成的唯一新草稿编号；直接进入新草稿确认页|
|退款红冲成功|红票号码、金额、原票关系；进入票据详情|
|邮件交付失败|提示站内文件仍可下载，并提供更新本次接收邮箱/重发入口|

用户通知只能引用用户有权查看的申请、票据和更正对象。普通成员无详情权限时仅收到“团队财务已处理”的脱敏状态，不包含申请号、发票号码、文件入口或他人抬头信息。

### 14.2 后台告警

系统初始阈值固定如下；财务配置管理员可通过版本化发布调整，发布前一直使用当前生效版本：

- 待审核超过 1 个工作日；
- 结果未知超过 30 分钟；
- 开票失败或红冲失败立即进入待办；
- 文件补拉连续失败 3 次；
- 全部交付渠道失败；
- 主体额度低于预警值；
- 日终对账出现任意差异。

## 15. API 与事件契约

### 15.1 管理端接口

|接口|用途|
|---|---|
|`GET /admin/invoices/applications`|申请列表、筛选、分页和权限裁剪|
|`GET /admin/invoices/applications/metrics`|按相同查询口径返回指标及 `as_of`|
|`GET /admin/invoices/applications/{id}`|申请详情及四维状态|
|`POST /admin/invoices/applications/{id}/claim`|领取审核|
|`POST /admin/invoices/applications/{id}/release`|释放审核锁|
|`POST /admin/invoices/applications/{id}/approve`|审核通过并在事务 Outbox 写入唯一首次开具命令|
|`POST /admin/invoices/applications/{id}/reject`|驳回并按规则释放占用|
|`GET /admin/invoices/provider-tasks/{id}`|开具/查询/文件任务详情与审计摘要|
|`POST /admin/invoices/provider-tasks/{id}/query`|主动查询第三方|
|`POST /admin/invoices/provider-tasks/{id}/retry`|确认可重试后重新提交|
|`POST /admin/invoices/provider-tasks/{id}/resume`|恢复审核通过后已创建但尚未执行的原排队任务，不创建新任务|
|`POST /admin/invoices/provider-tasks/{id}/close-for-correction`|提交未开票证据，关闭旧任务、释放占用并开放客户端补正入口；不自动创建新申请|
|`GET /admin/invoices/documents`|票据管理列表、筛选与分页|
|`GET /admin/invoices/documents/{id}`|票面、原票关系、文件、交付和金额详情|
|`POST /admin/invoices/documents/{id}/preview-authorizations`|为指定文件版本创建短时只读预览授权|
|`POST /admin/invoices/documents/{id}/download-authorizations`|为指定 PDF/OFD/XML 文件版本创建短时下载授权|
|`POST /admin/invoices/document-packages`|按选中票据/查询快照创建异步打包任务|
|`GET /admin/invoices/document-packages/{id}`|查询打包进度、失败明细和过期时间|
|`POST /admin/invoices/document-packages/{id}/download-authorizations`|为已完成 ZIP 创建一次性短时下载授权|
|`POST /admin/invoices/documents/{id}/refetch`|补拉票据文件|
|`POST /admin/invoices/documents/{id}/finance-corrections`|按锁内可红冲余额创建财务纠错 RedIntent/RedTask|
|`POST /admin/invoices/deliveries/{id}/resend`|重新交付|
|`GET /admin/invoices/red-tasks`|红冲任务列表|
|`GET /admin/invoices/red-tasks/{id}`|红冲来源、金额占用、确认、第三方和红票详情|
|`POST /admin/invoices/red-tasks/{id}/submit`|提交红冲|
|`POST /admin/invoices/red-tasks/{id}/query`|查询红冲结果；结果未知场景的唯一处置入口|
|`POST /admin/invoices/red-tasks/{id}/retry`|红冲失败重试|
|`POST /admin/invoices/red-tasks/{id}/cancel`|仅按 9.2.2 规则取消未提交的 `FINANCE_CORRECTION` 财务纠错任务；退款与用户更正来源拒绝|
|`POST /admin/invoices/red-tasks/{id}/confirmation-query`|向第三方查询受票方确认状态|
|`POST /admin/invoices/red-tasks/{id}/confirmation-register`|登记线下核验的确认结论、时间和凭证哈希|
|`GET /admin/invoices/correction-requests`|更正申请列表、筛选与分页|
|`GET /admin/invoices/correction-requests/{id}`|更正前后 Diff、校验、红冲和新草稿关联详情|
|`POST /admin/invoices/correction-requests/{id}/claim`|领取更正审核|
|`POST /admin/invoices/correction-requests/{id}/release`|释放更正审核锁|
|`POST /admin/invoices/correction-requests/{id}/approve`|审核通过并原子创建逐票红冲任务/占用|
|`POST /admin/invoices/correction-requests/{id}/reject`|按固定原因驳回，不修改用户资料|
|`POST /admin/invoices/offline-documents/upload-authorizations`|灾备模式下为已通过申请/原任务创建受限文件上传授权|
|`GET /admin/invoices/offline-documents`|灾备登记列表、筛选、分页和待复核工作台|
|`POST /admin/invoices/offline-documents`|登记外部发票号码、票面、关联分摊、文件哈希和开具凭证为待复核记录|
|`GET /admin/invoices/offline-documents/{id}`|查看线下登记及重复号码/金额/文件校验结果|
|`POST /admin/invoices/offline-documents/{id}/confirm`|由非上传人复核后写入正式 InvoiceDocument、账本和交付任务|
|`POST /admin/invoices/offline-documents/{id}/reject`|由非上传人驳回，保留审计但不写正式票据|
|`GET /admin/invoices/config/{resource}`|分页查询主体、内容、SKU 映射、服务商或通知配置及版本|
|`POST /admin/invoices/config/{resource}/validate`|发布前覆盖、引用、连接和冲突校验|
|`POST /admin/invoices/config/{resource}`|创建配置草稿|
|`PATCH /admin/invoices/config/{resource}/{id}`|按版本修改配置草稿|
|`POST /admin/invoices/config/{resource}/{id}/publish`|发布新版本并固化生效时间|
|`POST /admin/invoices/config/{resource}/{id}/disable`|经引用检查后停用配置|
|`POST /admin/invoices/exports`|基于查询快照创建导出任务|

所有写接口都接收 `request_id` 和操作原因；对已有对象执行状态/内容变更时必须同时接收对象 `version`。新建类接口使用 `request_id + payload_hash` 及对应业务唯一键保证幂等，不要求不存在对象的版本号。响应返回最新对象版本、相关状态与 `allowed_actions`。文件授权接口返回一次性 token、过期时间、文件版本和哈希，不返回对象存储真实路径。列表接口统一返回 `items/total/page/page_size/as_of`。

线下票据登记只在配置管理员开启灾备开关并填写事件单号后可用。蓝票必须关联已通过申请、未成功的原 ProviderTask 和逐订单分摊；红票必须关联原蓝票、有效 RedIntent/RedTask 和逐票 InvoiceRedAllocation。上传人与确认人必须为不同账号；这是灾备登记的固定职责分离，不扩展为日常申请审核的双人复核。确认前系统必须查询第三方、校验发票号码唯一性、票面金额等于待开/待红冲金额、文件哈希无重复；任一不通过即禁止确认。

### 15.2 领域事件

|事件|主要消费者|
|---|---|
|`invoice.application.submitted`|后台待审核、通知、指标|
|`invoice.application.withdrawn`|账本释放、审计|
|`invoice.application.approved`|第三方任务编排|
|`invoice.application.rejected`|账本释放、客户端通知|
|`invoice.issuance.submitted`|监控、主动查询调度|
|`invoice.issuance.queue_resumed`|任务编排、审计、监控|
|`invoice.issuance.correction_required`|开放客户端补正入口、通知；不自动创建申请|
|`invoice.document.issued`|文件获取、交付、订单关联|
|`invoice.document.offline_registered`|灾备复核待办、审计|
|`invoice.document.offline_confirmed`|正式票据入账、文件归档、交付|
|`invoice.issuance.failed`|财务待办、通知|
|`order.refund.approved`|未开票剔除或红冲任务创建|
|`invoice.red.required`|红冲工作台|
|`invoice.red.confirmation_required`|受票方通知、红冲待办|
|`invoice.red.completed`|账本重算、红票交付、更正草稿编排|
|`invoice.correction.submitted`|更正审核待办、通知、指标|
|`invoice.correction.approved`|逐票红冲任务编排、客户端进度|
|`invoice.correction.rejected`|客户端通知、审计|
|`invoice.correction.replacement_draft_created`|更正单转已完成、客户端新草稿入口与唯一通知|
|`invoice.correction.completed`|关联链审计、指标|
|`invoice.delivery.failed`|重发待办|

事件携带内部 ID 与版本，不携带完整税号、银行账号、邮箱或电话。

## 16. 安全、隐私与审计

- 税号、银行账号、邮箱、电话等字段传输加密、存储加密、列表脱敏。
- 完整字段临时查看必须有独立权限、二次确认、目的说明和审计。
- 表单敏感字段不得被 SystemFrame 自动写入 `sessionStorage`；原型/实现需增加明确的禁持久化标记。
- 第三方回调必须验签、防重放并限制来源；失败回调进入隔离队列。
- 下载/预览使用短时授权，禁止在 URL、埋点和前端日志中暴露文件存储路径。
- 用户侧申请/票据详情按申请人或团队角色权限鉴权，禁止因普通成员拥有任一订单分摊记录就返回跨成员合并申请；无详情权限时只下发不含申请/票据 ID 的脱敏处理状态。
- 操作日志至少记录操作者、角色、主体范围、动作、前后状态、对象版本、金额、原因、request ID、IP/设备摘要、结果和时间。
- 财务配置变更采用版本化发布；密钥不在应用数据库或页面明文保存。

## 17. 导出

- 导出范围为当前查询快照，服务端再次校验数据和字段权限。
- 默认字段：申请编号、四维状态、发票类型、受票方名称、脱敏税号、申请金额、开票主体、申请/审核/开票时间、发票号码、关联订单号、异常分类。
- 完整税号、邮箱、电话、银行账号默认不导出；确需导出使用更高权限和独立审批。
- 导出异步生成，文件加密、设定过期时间并记录下载审计。

## 18. 埋点与监控

|指标/事件|用途|
|---|---|
|申请量、审核通过率、驳回原因分布|产品与财务效率|
|审核等待时长、处理时长|SLA|
|第三方提交成功率、结果未知率、最终失败率|服务商质量|
|回调验签失败、重复事件、乱序事件|安全与接口稳定性|
|开票到文件可用时长|票据可用性|
|交付成功率、重发次数|用户交付质量|
|红冲触发量、完成率、耗时、失败原因|退款闭环|
|对账差异数与修复时长|财务一致性|

技术监控和产品埋点不得记录票面明文或敏感字段。

## 19. 验收标准

### 19.1 页面与权限

1. “财务管理 > 发票管理”路由、菜单高亮和模块加载符合后台现有 SystemFrame 架构。
2. 不同角色只能看到授权开票主体和 `allowed_actions`。
3. 申请管理、票据管理、红冲任务和配置 Tab 的边界清晰。
4. 列表筛选、分页、导出和详情在空态、失败态、窄屏下可用。

### 19.2 审核与开具

5. 领取审核、超时释放、审核通过、驳回均使用版本锁并记录审计。
6. 审核通过前服务端重新校验订单、支付、履约、退款、账本和税务配置。
7. 提交第三方后只显示“任务已提交/开票中”，不提前宣告成功。
8. 第三方明确失败、超时结果未知、重复/乱序回调和部分成功均按规则处理。
9. 同一第三方请求在重试和回调过程中不得生成重复票据。

### 19.3 票据、交付与红冲

10. 蓝票/红票可预览、下载并追溯原票关系；文件失败不改变开票成功事实。
11. 邮件失败只影响交付状态，并可受控重发。
12. 退款自动取消/剔除未开票申请或创建正确金额的红冲任务。
13. 红冲中和失败金额保持冻结，成功后仅按退款净额恢复资格。
14. 更正重开形成原申请、蓝票、红票和新申请的完整关联链。

### 19.4 一致性与安全

15. 任一订单的占用金额 + 有效蓝票金额不超过资格法币净额。
16. 申请、开具、票据、交付四维状态可独立查询并正确派生页面状态。
17. 日终对账可识别内部与第三方的票据、金额、状态和文件差异。
18. 敏感字段不进入浏览器持久化、URL、埋点或普通日志；预览、下载、查看明文均有审计。
19. 申请管理、票据管理、红冲任务/更正申请、开票配置四个 Tab 均按本文字段完成筛选、稳定排序、分页、详情深链和 `allowed_actions` 校验。
20. 多张蓝票/红票及站内/邮件交付单元按 11.3–11.5 重算；部分成功、部分红冲、红冲处理中和部分送达均显示正确金额，且不会互相覆盖状态维度。
21. 同一红冲来源业务重复消费只产生一个 RedIntent；同票成功红冲 + 有效占用永不超过原蓝票金额，且最多一个任务处于第三方活动阶段。
22. 用户更正申请的领取、释放、审核、驳回、撤回和系统推进均校验版本；审核员不能修改用户资料，驳回后以新 ID 形成版本链。
23. 用户更正的全部红冲成功后，无需人工或用户触发即可幂等生成唯一新申请草稿；事件重放不生成第二份草稿，草稿提交仍重新校验并进入人工审核。
24. 预览、单文件下载、异步打包、文件补拉、受票方确认查询/登记、线下票据上传/确认和更正审核接口均可按权限、幂等键和版本完成成功/失败用例。
25. 灾备线下登记只能关联真实订单及已通过申请，上传人与确认人分离；重复发票号码、重复文件、金额不符或第三方已有结果均禁止写入正式票据账本。

## 20. 上线依赖与发布门槛

- 财务确认开票主体、发票内容、税收分类编码和税率映射；
- 所有支持法币支付的在售 SKU 通过配置覆盖检查；
- 第三方沙箱完成蓝票、查询、回调、红冲、文件和异常用例；
- 订单、支付、履约、退款事件能够提供稳定 ID、版本和幂等保障；
- 权限中心完成财务权限点和主体数据范围；
- 文件归档、邮件交付、告警和日终对账通过演练；
- 完成重复提交、并发审核、同票并发红冲、第三方超时、回调重放、部分成功、退款红冲和更正自动建草稿压测/故障演练；
- 完成 PDF 预览、单文件下载、异步打包、受票方确认查询/登记和灾备线下登记的权限与审计演练；
- 财务、法务、安全、技术和测试共同签署上线检查表。
