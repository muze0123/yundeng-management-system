**核心布局结构**：整个分页器在数据列表下方居右
- **核心分页导航区（左侧）**：上一页按钮、当前页码按钮（深蓝色背景）、普通页码按钮、省略号、下一页按钮
- **每页条数选择区（中间）**：下拉选择框，显示"10条/页"，可选10/20/50/100条
- **跳转与数据统计区（右侧）**：跳转提示文字"跳至"、页码输入框、单位文字"页"、数据统计文字"共40条记录 第1/8页"

**关键交互**：
- 点击上一页/下一页按钮翻页
- 点击页码按钮跳转到指定页
- 下拉选择切换每页显示条数
- 输入框输入页码后按回车跳转
- 当前页码按钮为深蓝色背景，不可点击
- 第1页时上一页按钮不可点击，最后一页时下一页按钮不可点击

**样式规范**：
- 主色：深蓝色（#1677ff）用于当前页码
- 中性色：深灰（文字、图标）、浅灰（边框）、白色（背景）
- 字体：无衬线字体
- 圆角：所有按钮、输入框统一轻微圆角
- 间距：元素间均匀留白，垂直居中对齐

文档已生成完毕，包含完整的布局结构、交互说明、数据字典、边界条件、Toast提示、使用示例和注意事项。
用户要求我生成分页器组件的PRD交互说明文档，我已经完成了。我已经：
1. 识别了图片内容
2. 生成了完整的PRD交互说明文档
3. 调用了done工具完成任务

现在我需要向用户简洁说明核心功能。

现在我已经识别了图片内容，让我生成完整的PRD交互说明文档：

---

# 分页器组件_PRD交互说明

## 一、页面概述

### 1.1 页面定位
本组件为Web后台管理系统的标准分页器组件，用于数据列表的分页导航，支持页码切换、每页条数选择、跳转到指定页等操作，适用于所有需要分页展示的列表页面。

### 1.2 用户场景
- 用户在数据列表中翻页查看不同页的数据
- 用户切换每页显示的数据条数
- 用户跳转到指定页码
- 用户查看总数据量和当前页码信息

---

## 二、组件整体布局结构

### 2.1 整体布局（横向线性排列）

```
┌─────────────────────────────────────────────────────────────────────────────────┐
│  [<] [1] [2] [3] [4] [5] [...] [43] [>]    [10条/页▼]    跳至[5]页  共40条记录 第1/8页  │
└─────────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 区域详细布局

#### 2.2.1 核心分页导航区（左侧）
- **布局位置**：组件最左侧
- **排列方式**：横向排列
- **内容结构**：
  - 上一页按钮（<）
  - 当前页码按钮（1，深蓝色背景）
  - 普通页码按钮（2、3、4、5）
  - 省略号（...）
  - 最后一页按钮（43）
  - 下一页按钮（>）

#### 2.2.2 每页条数选择区（中间）
- **布局位置**：核心分页导航区右侧
- **排列方式**：单个下拉选择框
- **内容结构**：
  - 下拉选择框，显示"10条/页"

#### 2.2.3 跳转与数据统计区（右侧）
- **布局位置**：每页条数选择区右侧
- **排列方式**：横向排列
- **内容结构**：
  - 跳转提示文字："跳至"
  - 页码输入框：显示数字"5"
  - 单位文字："页"
  - 数据统计文字："共40条记录 第1/8页"

---

## 三、区域交互说明

### 3.1 核心分页导航区交互

#### 3.1.1 上一页按钮
- **组件类型**：翻页按钮
- **外观**：浅灰色圆角矩形边框，白色背景，内部左向箭头图标（<），深灰色
- **交互说明**：
  - 点击：跳转到上一页
  - 当前为第1页时，按钮不可点击（灰色状态）
  - 鼠标移入：高亮显示（可点击状态）

#### 3.1.2 当前页码按钮
- **组件类型**：页码按钮（当前选中）
- **外观**：深蓝色（#1677ff）实心背景，白色文字，边角轻微圆角
- **显示内容**：数字"1"
- **交互说明**：
  - 不可点击（当前页码）
  - 纯展示，标识当前所在页码

#### 3.1.3 普通页码按钮
- **组件类型**：页码按钮
- **外观**：浅灰色圆角矩形边框，白色背景，深灰色数字文字
- **显示内容**：数字"2"、"3"、"4"、"5"、"43"
- **交互说明**：
  - 点击：跳转到对应页码
  - 鼠标移入：高亮显示（背景变色）

#### 3.1.4 省略号
- **组件类型**：占位符
- **外观**：深灰色"..."
- **交互说明**：
  - 无交互性
  - 表示中间存在大量未展示的页码

#### 3.1.5 下一页按钮
- **组件类型**：翻页按钮
- **外观**：浅灰色圆角矩形边框，白色背景，内部右向箭头图标（>），深灰色
- **交互说明**：
  - 点击：跳转到下一页
  - 当前为最后一页时，按钮不可点击（灰色状态）
  - 鼠标移入：高亮显示（可点击状态）

---

### 3.2 每页条数选择区交互

#### 3.2.1 每页条数下拉选择框
- **组件类型**：下拉选择框
- **外观**：浅灰色圆角边框，白色背景，深灰色文字"10条/页"
- **交互说明**：
  - 点击：展开下拉选项（10条/页、20条/页、50条/页、100条/页）
  - 选择选项：切换每页显示条数，刷新列表数据
  - 鼠标移入：高亮显示

---

### 3.3 跳转与数据统计区交互

#### 3.3.1 跳转提示文字
- **组件类型**：提示文字
- **外观**：深灰色无衬线字体
- **显示内容**："跳至"
- **交互说明**：
  - 纯展示，无交互

#### 3.3.2 页码输入框
- **组件类型**：输入框
- **外观**：浅灰色圆角边框，白色背景，内部数字"1"
- **交互说明**：
  - 默认值：1，输入框始终不为空
  - 点击：聚焦输入框
  - 输入页码：输入目标页码
  - 按回车键：跳转到目标页，跳页成功后输入框重置为1（不清空）
  - 输入页码超出范围时红色文字提示"页码超出范围"
  - 输入非数字时红色文字提示"请输入有效页码"

#### 3.3.3 单位文字
- **组件类型**：单位文字
- **外观**：深灰色无衬线字体
- **显示内容**："页"
- **交互说明**：
  - 纯展示，无交互

#### 3.3.4 数据统计文字
- **组件类型**：统计文字
- **外观**：深灰色无衬线字体
- **显示内容**："共40条记录 第1/8页"
- **交互说明**：
  - 纯展示，无交互
  - 显示总数据量、当前页码/总页码数

---

## 四、数据字典

| 字段名称 | 字段类型 | 字段长度 | 必填 | 说明 | 默认值 |
|---------|---------|---------|------|------|--------|
| current_page | Integer | - | 是 | 当前页码 | 1 |
| page_size | Integer | - | 是 | 每页条数 | 10 |
| total_count | Integer | - | 是 | 总记录数 | 0 |
| total_pages | Integer | - | 是 | 总页数 | 0 |
| page_size_options | Array | - | 是 | 每页条数选项 | [10, 20, 50, 100] |

---

## 五、边界条件汇总

### 5.1 页码按钮边界条件
1. 总页数小于等于5时，显示所有页码按钮，不显示省略号
2. 总页数大于5时，显示前5个页码按钮和最后一个页码按钮，中间用省略号表示
3. 当前页码为第1页时，上一页按钮不可点击
4. 当前页码为最后一页时，下一页按钮不可点击
5. 当前页码按钮为深蓝色背景，不可点击

### 5.2 每页条数边界条件
1. 默认每页条数为10条
2. 可选项为10条/页、20条/页、50条/页、100条/页
3. 切换每页条数时，刷新列表数据，当前页码重置为第1页

### 5.3 跳转输入边界条件
1. 输入框默认值为1，始终不为空
2. 输入页码为非数字时，红色文字提示"请输入有效页码"
3. 输入页码小于1时，红色文字提示"页码超出范围"
4. 输入页码大于总页数时，红色文字提示"页码超出范围"
5. 输入页码有效时，跳转到目标页，跳页成功后输入框重置为1（不清空）

### 5.4 数据统计边界条件
1. 总记录数为0时，显示"共0条记录 第1/1页"
2. 总记录数超过1000时，显示"共1000+条记录"
3. 当前页码/总页码数格式为"第X/Y页"

---

## 六、Toast提示汇总

### 6.1 跳转Toast提示
1. 跳转页码超出范围：toast提示"页码超出范围"
2. 跳转页码无效：toast提示"请输入有效页码"
3. 跳转成功：toast提示"跳转成功"

### 6.2 通用Toast提示
1. 网络异常：toast提示"网络异常，请检查网络连接"
2. 加载失败：toast提示"加载失败，请重试"

---

## 七、使用示例

### 示例1：翻页查看
- **场景/输入**：用户点击下一页按钮
- **预期产出**：跳转到下一页，列表数据刷新
- **关键要点**：当前页码按钮变为深蓝色背景

### 示例2：跳转到指定页
- **场景/输入**：用户在页码输入框输入"5"，按回车键
- **预期产出**：跳转到第5页，列表数据刷新
- **关键要点**：输入有效页码后跳转

### 示例3：切换每页条数
- **场景/输入**：用户点击每页条数下拉选择框，选择"20条/页"
- **预期产出**：每页显示20条数据，列表数据刷新，当前页码重置为第1页
- **关键要点**：切换每页条数时刷新数据

### 示例4：点击页码按钮
- **场景/输入**：用户点击页码按钮"3"
- **预期产出**：跳转到第3页，列表数据刷新
- **关键要点**：点击页码按钮跳转

### 示例5：上一页/下一页
- **场景/输入**：用户点击上一页按钮
- **预期产出**：跳转到上一页，列表数据刷新
- **关键要点**：第1页时上一页按钮不可点击

### 示例6：输入无效页码
- **场景/输入**：用户在页码输入框输入"0"，按回车键
- **预期产出**：红色文字提示"页码超出范围"
- **关键要点**：输入无效页码时提示错误

---

## 八、注意事项

### 8.1 整体布局注意事项
1. 组件采用横向线性排列，所有元素在同一水平行内对齐
2. 从左到右分为三个功能模块：核心分页导航区、每页条数选择区、跳转与数据统计区
3. 元素之间保留均匀的留白间距，视觉上紧凑但不拥挤

### 8.2 核心分页导航区注意事项
1. 上一页按钮为浅灰色圆角矩形边框，白色背景，内部左向箭头图标
2. 当前页码按钮为深蓝色（#1677ff）实心背景，白色文字
3. 普通页码按钮为浅灰色圆角矩形边框，白色背景，深灰色数字文字
4. 省略号为深灰色"..."，表示中间存在大量未展示的页码
5. 下一页按钮为浅灰色圆角矩形边框，白色背景，内部右向箭头图标
6. 所有按钮尺寸一致，为正方形，边角有轻微圆角

### 8.3 每页条数选择区注意事项
1. 下拉选择框为浅灰色圆角边框，白色背景，深灰色文字
2. 默认显示"10条/页"
3. 可选项为10条/页、20条/页、50条/页、100条/页

### 8.4 跳转与数据统计区注意事项
1. 跳转提示文字为"跳至"，深灰色无衬线字体
2. 页码输入框为浅灰色圆角边框，白色背景
3. 单位文字为"页"，深灰色无衬线字体
4. 数据统计文字为"共40条记录 第1/8页"，深灰色无衬线字体

### 8.5 色彩规范注意事项
1. 主色为深蓝色（#1677ff），用于当前页码按钮
2. 中性色为深灰（文字、图标）、浅灰（边框、不可点击状态）、白色（背景）
3. 整体配色符合后台系统的简洁、专业调性

### 8.6 字体规范注意事项
1. 全页面使用无衬线字体（如微软雅黑、Inter等）
2. 保证可读性
3. 字号统一，层级清晰

### 8.7 圆角与边框注意事项
1. 所有可交互按钮、输入框都使用统一的轻微圆角
2. 边框宽度一致，视觉风格统一

### 8.8 间距注意事项
1. 所有元素的水平间距均匀
2. 垂直方向居中对齐
3. 整体视觉平衡有序

### 8.9 尺寸注意事项
1. 所有按钮、输入框的高度统一
2. 保证视觉上的整齐感